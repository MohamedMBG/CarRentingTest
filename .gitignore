*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Secrets — never commit
# Why: contains Firebase API key + sender id; must be provisioned per-env via Firebase Console or CI secret.
app/google-services.json
google-services.json

# Environment overrides — local-only
.env
.env.*
!.env.example
